# ZeroVector 多模型管理优化方案

## 问题背景

原始实现中，每个LLM服务需要为不同的使用场景（摘要生成、文档映射、导航查询）分别注入ChatClient，导致配置类中需要注入多个客户端，代码显得冗余且不易维护。

## 优化方案

我们引入了`MultiModelManager`类，它负责管理不同场景下的模型客户端，支持根据使用场景自动选择合适的模型。这样可以将多个客户端的创建和管理逻辑封装到一个类中，简化配置类的代码。

### 核心组件

1. **MultiModelManager**: 多模型管理器，负责根据场景创建和管理ChatClient实例
2. **Scenario枚举**: 定义了三种使用场景：摘要生成(SUMMARY)、文档映射(MAP)、导航查询(NAVIGATION)
3. **优化的SpringAiLLMService**: 使用MultiModelManager获取不同场景的客户端，而不是直接注入多个客户端

### 优势

1. **简化配置**: 配置类中只需要注入一个MultiModelManager，而不是多个ChatClient
2. **延迟初始化**: 客户端按需创建，提高启动性能
3. **易于扩展**: 新增场景只需在Scenario枚举中添加新值，并在MultiModelManager中添加对应方法
4. **统一管理**: 所有模型客户端的创建逻辑集中在一个地方，便于维护

### 配置示例

```yaml
zerovector:
  model:
    # 摘要生成使用的模型（可以使用较便宜的模型以降低成本）
    summary-model: "gpt-3.5-turbo"
    
    # 文档映射/聚类使用的模型（需要较好的理解能力）
    map-model: "gpt-4"
    
    # 导航查询使用的模型（需要较高的准确性）
    navigation-model: "gpt-4-turbo"
```

### 使用方式

1. 在配置类中，我们创建MultiModelManager Bean：
```java
@Bean
@ConditionalOnMissingBean
public MultiModelManager multiModelManager(ChatModel chatModel, ZeroVectorProperties config, SpringAiModelProvider modelProvider) {
    return new MultiModelManager(chatModel, config, modelProvider);
}
```

2. 在LLMService中，我们注入MultiModelManager并根据场景获取客户端：
```java
// 获取摘要场景的客户端
ChatClient summaryClient = multiModelManager.getSummaryClient();

// 获取映射场景的客户端
ChatClient mapClient = multiModelManager.getMapClient();

// 获取导航场景的客户端
ChatClient navigationClient = multiModelManager.getNavigationClient();
```

### 扩展指南

如果需要支持新的场景，可以按照以下步骤：

1. 在`MultiModelManager.Scenario`枚举中添加新场景
2. 在`MultiModelManager`中添加获取新场景客户端的方法
3. 在`SpringAiModelProvider`的实现中处理新场景的模型配置
4. 更新配置属性类，添加新场景的模型配置项

这种设计使得系统更加灵活，可以根据不同场景的需求选择最合适的模型，从而在成本和效果之间取得平衡。