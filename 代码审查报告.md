# ZeroVector 代码审查报告

- **审查日期**：2026-08-21
- **审查范围**：`zerovector-core` 全部主要实现（导航、树构建、存储、缓存、索引、Hook）、`zerovector-spring-boot-starter`（自动配置、SemanticHub、LLM Provider）
- **审查方式**：逐文件静态阅读 + 运行时验证（示例应用实际编译、安装、启动测试）
- **严重级别**：🔴 Blocking（建议尽快修复） / 🟡 Important（应修复或明确决策） / 🟢 Nit（不阻塞） / 💡 Suggestion（方案建议）

---

## 修复记录（2026-08-21）

以下问题已修复并通过全量测试回归（173 个用例，0 失败）：

| 编号 | 问题 | 修复方式 |
|------|------|----------|
| 🔴1 | chunkCache 并发读写 | `HashMap` → `ConcurrentHashMap` |
| 🔴2 | Navigator 有状态共享 | 导航状态改为 `navigate()` 局部变量；解析失败改为抛异常与 HybridNavigator 对齐 |
| 🔴3 | `abs(hash)%n` 越界 | 改为 `Math.floorMod` |
| 🟡4 | example 加载旧 jar 启动失败 | README / AGENTS.md 补充"先 install 再运行"说明 |
| 🟡5 | docId 同毫秒冲突 | 时间戳 → UUID |
| 🟡6 | 并发上传无保护 | `addDocument` / `buildTree` 加 `synchronized` |
| 🟡7 | 删除语义不完整 | `deleteChunk` 级联删除 mmap 数据（分片文件按条删除的限制已在 Javadoc 注明） |
| 🟡8 | 树持久化非原子 | metadata 改 temp+rename 原子替换并最后写入（作为提交点）；懒加载增加"刚加载分片数据兜底"，消除驱逐窗口误报 null |
| 🟡9 | 模糊缓存错误命中 | 相似匹配仅对确定性提取类请求启用（关键词/实体/示例问题/查询抽词），理解/聚类/摘要/导航决策禁用 |
| 🟡10 | 中文 fallback 失效 + 归一化不对称 | 精确匹配失败时退化为关键词包含匹配（过滤单字防误报）；`removeKeywordDefinition` 补 toLowerCase |
| 🟡11 | 索引持久化脆弱 | saveIndex 改 temp+rename 原子写；类 Javadoc 标注 2GB 上限 |
| 🟡12 | close() 无锁 unmap | close 全程持有写锁 |
| 🟢13 | invokeAll 忽略超时 | 实现完整超时语义（等待+取消未完成任务），无参版本复用之 |
| 🟢15 | expandBuffer 冗余拷贝 | 数据已由 mmap 落盘，扩容改为 setLength+重映射，去除整块拷贝 |
| 🟢16 | HookRegistry 读无同步 | `ConcurrentHashMap` + `CopyOnWriteArrayList` |
| 🟢17 | 小列表 parallelStream | 两个 Navigator 改回 stream |
| 💡19 | SPI 单例陷阱 | findProvider 改为每次反射创建新实例；加载阶段改用 `ServiceLoader.Provider::type` 避免无谓实例化 |
| 💡20 | 限流器按实例隔离导致聚合 RPS 放大 | RateLimiter 按 rps 值全局共享（Semaphore 并发度仍按实例隔离）；顺带修复 rps≤0 时除零产生无限等待的问题 |
| 🟢18 | SmartCacheStrategy 静态可变配置 | 新增不可变 `SimilarityConfig`（record，构造时裁剪非法值），`isSimilarPrompt` 改为携带配置的三参方法；`CachedLLMProvider` 按实例持有配置并新增三参构造器；auto-config 改为构造器注入，移除全部静态 setter 调用。同一 JVM 内多个 provider / 多个 Spring 上下文不再互相覆盖 |

**评估后暂不修改**：

- 🟢14（中断绕过限流）：中断意味着调用方要求尽力而为，继续执行属可接受的降级策略。

---

## 死代码清理记录（2026-08-21）

对全仓库做了逻辑死代码与不可达分支排查，以下内容已删除（全模块编译通过，168 个测试回归通过）：

### 永远走不到的分支

| 位置 | 问题 | 处理 |
|------|------|------|
| `NavigationAction` | 原 `SelectLeaves`/`ExpandMultiple`/`FallbackSearch` 三个 sealed 变体全仓库无构造点，提示词与解析器均未接线 | **改为完整接线（见下文"导航协议扩展"）** |
| `HybridNavigator` switch | 原 `case ExpandMultiple`/`case FallbackSearch`/`default` 不可达 | 接线后 switch 对 4 个变体穷尽，无 default |
| `SemanticTreeManager.navigate()` | `hybridNavigator` 与旧 navigator 总是成对创建且优先使用前者，`else if (navigator != null)` 与 `else throw NO_NAVIGATOR` 永不可达 | 简化为空值防御 + 单一调用路径 |
| `DocumentComprehender.unmapBuffer` | 大文件分块用 READ_ONLY 模式映射，对其调 `buffer.force()` 必抛 `ReadOnlyBufferException` | 移除 force() 调用 |

### 导航协议扩展（原死代码接线）

原 `NavigationAction` 的多分支/降级动作并非垃圾，而是未接线的能力脚手架。已完整接线：

- `ExpandMultiple`（多分支并行）：扩展 `decideNavigation` 提示词，LLM 可输出 `{"action":"expand_multiple","selectedIndexes":[0,2],...}`；解析器映射为 `ExpandMultiple`，`handleMultiPath` 收集各选中子节点子树下的全部叶子文档块（纯树遍历、不追加 LLM 调用），提升有歧义/跨类目查询的召回
- `FallbackSearch`（LLM 主动降级）：LLM 可输出 `{"action":"fallback_search",...}` 主动触发关键词倒排兜底，粒度比程序化的 confidence<0.3 判断更细
- `SelectLeaves`（按 chunkId 直选叶子）**未还原**：LLM 在提示词中看不到 chunkId（`doc_<uuid>`），该动作必然幻觉；其"多叶子结果"意图已由 `ExpandMultiple` 覆盖，保留会造成新的死代码
- 协议向后兼容：`NavigationDecisionResult` 新增 `action`/`selectedIndexes` 字段（均可空），无 `action` 字段时退化为旧 `selectedIndex` 逻辑，旧调用方不受影响

### 生产零调用的死代码

| 位置 | 问题 | 处理 |
|------|------|------|
| `tree/Navigator.java` 整个类 | `SemanticTreeManager` 三处构造但从不用于导航（永远走 HybridNavigator）；无测试引用；`getCurrentNode()` 零调用 | 删除整个类及 `navigator` 字段、三处构造、`resetNavigator()` 方法 |
| `ShardedTreeStorage.updateTreeIncremental` | 生产代码只走 `saveTree` 全量保存，此方法仅测试调用（且存在"已修改节点不落盘"的潜在缺陷） | 连同其测试一并删除 |
| `KeywordDictionary.getTopCandidate` | 仅测试调用 | 连同 2 个测试一并删除 |
| `SmartCacheStrategy.generateCacheKey(prefix, ...)` 重载 | 仅测试调用（生产用 RequestType 重载） | 连同测试一并删除 |
| `HookType.TREE_NODE_CREATED` | 从未被任何代码触发，注册到该类型的 Hook 永远不会执行 | 删除枚举值 |
| `ZeroVectorProperties.Storage` 配置树 | `zerovector.storage.*` 属性被绑定但无任何消费者（自动配置只用 `storage-base-path`），且嵌套类与 core 的 `storage.config.ChunkStorageConfig` 同名易混淆 | 删除 Storage 字段/getter 及三个嵌套配置类 |
| `LocalFileChunkStorage.getMMapStore()/getShardedStorage()` | 注释"供内部使用"，实际零调用 | 删除 |

### 排查后保留

| 位置 | 原因 |
|------|------|
| `MMapDocumentStore.compact()` / `ShardedMMapStore.compact()` | 生产代码暂无调用入口，但属合理的运维维护操作（mmap 追加日志需要空间回收），保留待未来暴露管理端点 |
| 异常体系的 `ErrorLevel` 分级机制 | 所有抛出点目前恒为 ERROR，分级参数化构造器未被使用；但作为异常语义的一部分保留，成本极低 |
| `getKeywordDictionary()` 的 UnsupportedOperationException 分支 | 当前默认构造路径不可达，但自定义 DictionaryStorage 注入时会触发——属潜在陷阱而非死代码，已在审查报告中另行标注 |

---

## 🔴 Blocking

### 1. `LocalFileChunkStorage.chunkCache` 是普通 HashMap，被并发读写

**位置**：`LocalFileChunkStorage.java:66`

```java
private final Map<String, DocumentChunk> chunkCache = new HashMap<>();
```

导航时 `HybridNavigator.loadChunks` 用 `parallelStream()` 并发调用 `getChunk`（读+写缓存，`:143` / `:152`），上传时 `saveChunk` 写入同一 Map，全程无同步。并发场景下存在数据竞争（丢失更新、ConcurrentModificationException）。

这与系统"虚拟线程 + parallelStream"的并发设计直接矛盾，属于设计上明确支持并发却未做保护的路径。

**修复**：改为 `ConcurrentHashMap`，一行改动。

---

### 2. `Navigator` 有状态且被所有查询共享

**位置**：`Navigator.java:54-55`

```java
private TreeNode currentNode;
private final List<NavigationPath> navigationHistory;
```

两个字段在 `navigate()` 中变更，而 `SemanticTreeManager` 持有单例 Navigator 供全部查询使用：

- 并发查询互相污染导航起始位置；
- 更严重的是 `navigationHistory` 跨查询累积（上限 100 条）并随每个 `NavigationResult` 返回——**查询 A 的路径会泄漏进查询 B 的结果**。

默认走无状态的 `HybridNavigator` 掩盖了问题，但 `Navigator` 是公开 API 且每次 buildTree 都会被创建（`SemanticTreeManager.java:527`）。

**修复**：将状态改为 `navigate()` 的局部变量；或明确文档标注 Navigator 非线程安全并每次查询新建实例。

---

### 3. `ShardedMMapStore.shardFor` 的 `Math.abs(hash) % n` 经典缺陷

**位置**：`ShardedMMapStore.java:69`

```java
return Math.abs(chunkId.hashCode()) % shards.length;
```

当 `hashCode() == Integer.MIN_VALUE` 时 `Math.abs` 结果仍为负数 → 数组下标越界异常。概率极低但真实存在，且表现为崩溃而非降级。

**修复**：`Math.floorMod(chunkId.hashCode(), shards.length)`。

---

## 🟡 Important

### 4. example 模块从本地仓库解析 SNAPSHOT，旧 jar 导致启动失败（已实测复现）

**现象**：未执行 install 直接在 example 目录运行 `spring-boot:run` 时，Maven 从本地仓库加载旧版 starter jar，启动即报：

```
NoSuchMethodError: ChatClient$Builder.defaultOptions(ChatOptions)
  at cn.nexon.zerovector.springboot.provider.SpringAiLLMProvider.<init>(...)
```

重新执行 `./mvnw install -DskipTests` 后应用正常启动（Tomcat 8080，1.6s 启动完成）。

这不是代码 bug，但是高频踩坑点：**修改 core/starter 后不 install 就运行 example，必然使用旧类**。

**建议**：README 与 AGENTS.md 注明先 `./mvnw install` 或使用 `./mvnw -pl zerovector-spring-boot-example -am spring-boot:run`。

### 5. `docId = "doc_" + System.currentTimeMillis()` 同毫秒冲突

**位置**：`SemanticTreeManager.java:459`

LLM 缓存命中时单文档处理可亚毫秒完成，批量/并发上传时同毫秒 ID 冲突 → `comprehendResultMap` / `chunkMap` 相互覆盖，**静默丢失文档**。树节点 ID 均用 UUID，此处却用时间戳，不一致。

**修复**：改用 `UUID.randomUUID()`。

### 6. 并发上传无保护

`addDocumentAsync` / `addDocumentsAsync` 是公开 API，但：

- `semanticTree` 字段非原子替换；
- `updateTree` → `saveTree` 全程无锁。

两个并发上传可互相覆盖语义树，或在遍历树时抛 CME。

**建议**：对 addDocument 全流程加锁，或串行化写入队列。

### 7. 删除语义不完整

**位置**：`LocalFileChunkStorage.deleteChunk(:192)`、`clear()(:247)`

只删内存缓存，mmap 存储与 shardedStorage 数据仍在——缓存失效后"已删除"的 chunk 复活。

**建议**：级联删除 mmapStore / shardedStorage 中的数据，或将方法重命名为 `evictFromCache` 以明示语义。

### 8. `ShardedTreeStorage` 持久化非原子

**位置**：`ShardedTreeStorage.saveTree(:96)`、`getNode(:207)`

- saveTree 顺序直写 root.json / node_*.json / metadata.json，无 temp+rename，中途崩溃留下不一致树。`:98` 的 `[CRITICAL]` 注释说明已经踩过一次顺序坑；
- getNode 先 `loadNodeShard` 再 `getIfPresent` 两步非原子，缓存驱逐压力下可能返回 null，使节点"消失"。

**建议**：metadata 写入改 temp+rename；getNode 改用 `cache.get(key, k -> load)` 原子加载模式。

### 9. 相似提示词模糊缓存有正确性风险

**位置**：`CachedLLMProvider.findSimilarCachedResult(:150)`、`SmartCacheStrategy.calculateSimilarity(:134)`

Levenshtein 相似度 ≥0.85 就把**别的 prompt 的响应**当本次结果返回，且相似度只比较前 500 字符（长 prompt 截断），前缀相同的长 prompt 会误命中。对 `COMPREHEND_CHUNK` / `DECIDE_NAVIGATION` 这类语义敏感请求等于静默返回错误答案。

此外每次 cache miss 线性扫描最多 100 条 × O(n²) 编辑距离，热路径 CPU 开销可观。

**建议**：默认仅对确定性任务（关键词/实体提取类）启用相似匹配；导航决策与文档理解禁用。

### 10. 中文查询下 fallback 路径基本失效

**位置**：`KeywordDictionary.matchCandidates(:129)`

按 `\s+` 分词，中文整句成为一个"词"，倒排索引几乎必 miss。fast-track 靠 LLM 抽取关键词缓解，但 `HybridNavigator.handleFallback` 对中文用户是死路。

另：`removeKeywordDefinition(:275)` 未做 `toLowerCase`，与 `addEntry` 的归一化不对称，混合大小写关键词删除静默失败。

### 11. mmap 索引持久化脆弱

**位置**：`MMapDocumentStore.saveIndex(:384)`、`loadIndex(:352)`

- saveIndex 直接覆写目标文件，非原子——崩溃即索引损坏（数据文件完好但全部不可达）；
- CSV 格式对含逗号的 chunkId 无防护（当前 `doc_时间戳` 格式安全，但 SPI 公开接口无约束）；
- offset 到处强转 int，文件 >2GB 时静默出错。

**建议**：saveIndex 改 temp+rename；文档标注 2GB 上限。

### 12. `close()` 不取锁直接 unmap buffer

**位置**：`MMapDocumentStore.close(:407)`

未获取读写锁就执行 `buffer.force()` + `MmapUtils.clean(buffer)`。进行中的 `getChunk` 若访问已 unmap 的 buffer，存在 SIGSEGV 级 JVM 崩溃风险。

**建议**：close 内获取写锁后再清理。

---

## 🟢 Nit（不阻塞）

| # | 位置 | 问题 |
|---|------|------|
| 13 | `LLMExecutors.java:85` | `invokeAll(tasks, timeout, unit)` 静默忽略超时参数，违反 ExecutorService 契约 |
| 14 | `RateLimitedLLMProvider.java:49` | acquire 被中断后未获取许可仍继续调用 delegate，限流被绕过 |
| 15 | `MMapDocumentStore.expandBuffer(:326)` | 全量拷贝旧 buffer 冗余——文件内容还在，setLength+remap 即可，GB 级文件白拷一次 |
| 16 | `HookRegistry.trigger(:74)` | 读普通 HashMap 无同步（register 有锁）；初始化后只读，风险低 |
| 17 | `HybridNavigator/Navigator` | 单元素 chunkIds 也走 parallelStream，开销大于收益 |
| 18 | `SmartCacheStrategy:31-53` | 阈值是全局静态可变状态，多实例/测试互相污染 |

## 💡 Suggestion

| # | 位置 | 建议 |
|---|------|------|
| 19 | `StorageProviderFinder.findProvider(:62)` | 返回**共享单例**，而有状态的 storage 实现二次 initialize 会被拒绝。目前无人调用，但作为公开 SPI 入口是陷阱，应每次 newInstance |
| 20 | `LLMExecutors.create` | 限流器每个 SemanticTreeManager/TreeBuilder 各一份，多知识库聚合 RPS 成倍放大，考虑共享实例 |
| 21 | 两个 Navigator | 解析失败行为不一致：HybridNavigator 抛异常，Navigator 默认选第一个子节点（伪造 confidence=0.5，静默错误结果）。建议统一为抛异常 |

---

## 🎉 值得肯定的设计

- **降级链干净**：`HybridNavigator` 的 fast-track 设计层层兜底——LLM 抽词 → 字典匹配 → 根节点起点 → fallback，任何一层失败都有出路；
- **踩坑知识沉淀**：`ShardedTreeStorage.saveTree:98` 的 `[CRITICAL]` 注释记录了懒加载 Map 物化顺序的真实教训，正是这类注释避免后人重蹈覆辙；
- **可观测性好**：`LLMUsageStats` 贯穿 comprehend / build / navigate 全链路；Hook 异常隔离（单个 hook 失败不影响主流程）；
- **异常体系规范**：自定义异常带 errorCode（如 `NavigationException.ERROR_CODE_TREE_NOT_INITIALIZED`），语义清晰。

---

## 总结

整体架构思路清晰、分层合理（core 无 Spring 依赖、SPI 解耦存储、双 Navigator 分工）。核心风险集中在两条线：

1. **并发假设与实现不符**：🔴1 / 🔴2 / 🟡6 —— 代码大量使用虚拟线程与 parallelStream，但共享可变状态未做保护；
2. **持久化原子性**：🟡8 / 🟡11 / 🟡12 —— 多文件顺序写 + 直接覆写 + 无锁 close，崩溃恢复能力弱。

🔴 三项均为小改动大收益（一行 ConcurrentHashMap、状态局部化、floorMod），建议优先处理。
