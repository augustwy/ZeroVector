# ZeroVector 技术方案与实现指南

本文档详细描述 ZeroVector 项目的技术方案和实现思路，帮助工程师快速上手和学习本项目。

## 目录

- [一、项目概述](#一项目概述)
- [二、核心设计理念](#二核心设计理念)
- [三、系统架构](#三系统架构)
- [四、核心模块详解](#四核心模块详解)
- [五、关键技术实现](#五关键技术实现)
- [六、Java 25 特性应用](#六java-25-特性应用)
- [七、性能优化策略](#七性能优化策略)
- [八、开发指南](#八开发指南)
- [九、测试指南](#九测试指南)
- [十、故障排查](#十故障排查)
- [附录](#附录)

---

## 一、项目概述

### 1.1 项目背景

传统的知识库系统通常采用向量切片方案，将文档分割成固定大小的片段，然后使用向量数据库进行相似度检索。这种方案存在以下问题：

1. **上下文丢失**：文档被切碎后，大模型无法获取完整的上下文信息
2. **语义失真**：切片边界可能破坏语义完整性
3. **成本高昂**：需要 GPU 进行向量训练，维护向量数据库
4. **性能瓶颈**：向量检索在大规模数据下性能下降明显

### 1.2 解决方案

ZeroVector 采用了全新的设计思路：

- **零拷贝**：使用 mmap 技术，文档内容直接映射到虚拟内存
- **零失真**：保持文档的原始章节结构，不进行切片
- **零向量**：不使用向量数据库，直接基于语义树导航

### 1.3 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| JDK | 25+ | 基础运行环境 |
| Maven | 3.9+ | 项目构建工具 |
| LangChain4j | 最新 | LLM 集成框架 |
| Jackson | 最新 | JSON 序列化 |
| Spring Boot | 3.x | 应用框架（可选） |

---

## 二、核心设计理念

### 2.1 语义树结构

ZeroVector 的核心思想是将文档组织成语义树结构，树的每个节点代表一个语义单元（如章节、主题）。

```
Root (根节点)
├── 创建型模式 (分类节点)
│   ├── 单例模式 (叶子节点)
│   ├── 工厂模式 (叶子节点)
│   └── 建造者模式 (叶子节点)
├── 结构型模式 (分类节点)
│   ├── 适配器模式 (叶子节点)
│   └── 装饰器模式 (叶子节点)
└── 行为型模式 (分类节点)
    ├── 观察者模式 (叶子节点)
    └── 策略模式 (叶子节点)
```

### 2.2 混合导航策略

结合关键词匹配和 LLM 语义推理，实现精准导航：

1. **Phase 1: 关键词匹配** - 快速定位相关节点
2. **Phase 2: LLM 语义决策** - 基于语义理解选择路径
3. **Phase 3: 文档内容读取** - 从 mmap 文件读取完整内容

### 2.3 零拷贝存储

使用 mmap 技术将文件映射到虚拟内存，实现零拷贝读取：

```
文件系统 -> mmap -> 虚拟内存 -> 应用程序
                    ↓
              无需复制到堆内存
```

---

## 三、系统架构

### 3.1 分层架构

```
┌─────────────────────────────────────────┐
│         应用层 (Application)            │
│  SemanticFacade / CLI / Web API      │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│         服务层 (Service)                │
│     SemanticTreeManager                  │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│         业务层 (Business)               │
│  TreeBuilder / HybridNavigator          │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│         存储层 (Storage)                 │
│  MMapDocumentStore / ShardedTreeStorage  │
└─────────────────────────────────────────┘
```

### 3.2 数据流

#### 离线构建流程

```
原始文档 -> DocumentProcessor -> DocumentChunk
                                    ↓
                            TreeBuilder
                                    ↓
                            LLM 聚类与提取
                                    ↓
                    ┌───────────────┴───────────────┐
                    ↓                               ↓
            Semantic Tree (JSON)          Keyword Dictionary
                    ↓                               ↓
            ShardedTreeStorage              保存到文件
                    ↓
            MMapDocumentStore
```

#### 在线查询流程

```
用户查询 -> HybridNavigator
                    ↓
        ┌───────────┴───────────┐
        ↓                       ↓
KeywordDictionary          LLM 语义推理
        ↓                       ↓
    快速跳转              选择路径
        ↓                       ↓
        └───────────┬───────────┘
                    ↓
            MMapDocumentStore
                    ↓
            返回文档内容
```

---

## 四、核心模块详解

### 4.1 领域模型 (core.model)

#### TreeNode - 语义树节点

```java
public record TreeNode(
    String id,                          // 节点唯一标识
    String name,                        // 节点名称
    String description,                 // 节点描述
    NodeType type,                      // 节点类型
    List<String> childrenIds,           // 子节点ID列表
    List<String> chunkIds,             // 文档块ID列表
    List<String> keyEntities,           // 核心实体
    List<String> keywords,             // 关键词
    List<String> exampleQuestions      // 示例问题
) {}
```

**设计要点**：
- 使用 Record 实现不可变数据载体
- 包含 `keyEntities` 和 `keywords` 字段，用于关键词匹配
- `exampleQuestions` 帮助 LLM 理解节点语境

#### DocumentChunk - 文档块

```java
public record DocumentChunk(
    String id,                          // 文档块ID
    String content,                     // 文档内容（可为null）
    String summary,                     // 文档摘要
    String filePath,                    // 文件路径（基于文件路径的文档块）
    String md5,                         // MD5哈希值
    Map<String, Object> metadata        // 元数据
) {}
```

**设计要点**：
- 支持两种模式：基于内容和基于文件路径
- 基于文件路径的文档块不存储 `content`，节省存储空间
- MD5 用于重复文档检测

#### NavigationAction - 导航动作

```java
public sealed interface NavigationAction 
    permits SelectChild, ExpandMultiple, SelectLeaves, FallbackSearch, Stop {
    
    record SelectChild(String nodeId, String reasoning, double confidence) 
        implements NavigationAction {}
    
    record ExpandMultiple(List<String> nodeIds, String reasoning) 
        implements NavigationAction {}
    
    record SelectLeaves(List<String> chunkIds) 
        implements NavigationAction {}
    
    record FallbackSearch(String reason) 
        implements NavigationAction {}
    
    record Stop(String reasoning) 
        implements NavigationAction {}
}
```

**设计要点**：
- 使用 Sealed Interface 严格约束 LLM 的输出行为
- 每种动作都包含推理过程，便于调试

### 4.2 存储引擎 (core.storage)

#### MMapDocumentStore - 内存映射文档存储

**核心功能**：
- 将文档内容映射到虚拟内存
- 维护文档块索引（ChunkID -> FileLocation）
- 支持零拷贝读取

**关键实现**：

```java
public String getChunk(String chunkId) {
    FileLocation loc = index.get(chunkId);
    if (loc == null) return null;
    
    // 使用 slice 创建独立视图，避免多线程竞争 buffer position
    synchronized (this) {
        MappedByteBuffer sliceBuffer = buffer.duplicate();
        sliceBuffer.position((int) loc.offset());
        
        // 读取长度前缀
        int length = sliceBuffer.getInt();
        
        // 读取实际内容
        byte[] bytes = new byte[length];
        sliceBuffer.get(bytes);
        
        return new String(bytes, StandardCharsets.UTF_8);
    }
}
```

**线程安全**：
- 使用 `synchronized` 保护 buffer 位置
- 使用 `duplicate()` 创建独立视图
- 避免多线程并发读取导致 Buffer 指针错乱

#### ShardedTreeStorage - 分片语义树存储

**核心功能**：
- 将大型语义树拆分为多个小文件
- 按需加载，减少 I/O 和内存占用
- 支持增量更新

**文件结构**：

```
storage_dir/
├── metadata.json          # 元数据，包含分片索引信息
├── root.json             # 根节点（单独存储）
├── node_0.json           # 节点分片0
├── node_1.json           # 节点分片1
├── chunk_0.json          # 文档块分片0
└── chunk_1.json          # 文档块分片1
```

**分片策略**：
- 固定大小分片（默认 100 个节点/块）
- 元数据索引维护 ID 到分片文件的映射
- 懒加载：首次访问时才加载对应分片

### 4.3 语义树构建器 (core.tree.TreeBuilder)

**核心功能**：
- 使用虚拟线程并发处理文档摘要
- 递归构建树结构
- 自动提取关键词和实体

**构建流程**：

```java
public SemanticTree build(List<DocumentChunk> chunks) {
    // 使用虚拟线程并发处理文档摘要
    try (var executor = Executors.newVirtualThreadPerTaskExecutor()) {
        var futures = chunks.stream()
            .map(c -> executor.submit(() -> summarizeAndExtract(c)))
            .toList();
        
        // 等待所有摘要完成
        futures.forEach(f -> {
            try {
                f.get();
            } catch (Exception e) {
                logger.error("摘要生成失败", e);
            }
        });
    }
    
    // 递归构建树
    TreeNode root = buildRecursive("Root", chunks);
    
    // 持久化树和词典
    saveTree(root);
    saveDictionary(dictionary);
    
    return new SemanticTree(root);
}
```

**并发优化**：
- 使用 `Executors.newVirtualThreadPerTaskExecutor()` 创建虚拟线程
- 虚拟线程非常轻量，可以轻松创建数万个
- 并发处理提高构建速度

### 4.4 混合导航器 (core.navigator.HybridNavigator)

**核心功能**：
- 结合关键词匹配和 LLM 语义推理
- 实现精准导航
- 支持多路径扩展和回退搜索

**导航流程**：

```java
public NavigationResult navigate(String query) {
    // Phase 1: 关键词匹配 (快速通道)
    Map<String, Double> candidates = dictionary.matchCandidates(query);
    String fastTrackNodeId = null;
    
    if (!candidates.isEmpty()) {
        fastTrackNodeId = Collections.max(
            candidates.entrySet(), 
            Map.Entry.comparingByValue()
        ).getKey();
    }
    
    // Phase 2: LLM 语义决策
    TreeNode currentNode = (fastTrackNodeId != null) 
        ? tree.getNode(fastTrackNodeId) 
        : tree.rootNode();
        
    return navigateInternal(query, currentNode, new ArrayList<>());
}
```

**Prompt 构建**：

```java
private String buildNavigationPrompt(String query, TreeNode node) {
    String keywordHints = String.join(", ", node.keywords());
    String entityHints = String.join(", ", node.keyEntities());
    
    return String.format("""
        用户问题: %s
        当前节点: %s
        节点描述: %s
        该节点核心实体: %s
        该节点关键词: %s
        
        请判断应该进入哪个子节点？
        """, query, node.name(), node.description(), entityHints, keywordHints);
}
```

### 4.5 关键词词典 (core.index.KeywordDictionary)

**核心功能**：
- 维护关键词倒排索引
- 支持快速关键词匹配
- 解决 LLM 不认识专有名词的问题

**倒排索引结构**：

```java
// Keyword -> List<NodeId>
private final Map<String, List<String>> invertedIndex = new ConcurrentHashMap<>();

// Keyword -> Weight (用于排序)
private final Map<String, Double> keywordWeights = new ConcurrentHashMap<>();
```

**匹配算法**：

```java
public Map<String, Double> matchCandidates(String query) {
    Map<String, Double> candidateScores = new HashMap<>();
    
    // 简单分词匹配
    Arrays.stream(query.split("\\s+"))
        .filter(invertedIndex::containsKey)
        .forEach(kw -> {
            double weight = keywordWeights.getOrDefault(kw, 1.0);
            invertedIndex.get(kw).forEach(nodeId -> 
                candidateScores.merge(nodeId, weight, Double::sum)
            );
        });
        
    return candidateScores;
}
```

### 4.6 文档处理器 (core.document)

**核心功能**：
- 支持多种文档格式（Markdown, Text）
- 灵活的文档分块策略
- 自动计算 MD5 哈希值

**分块配置**：

```java
public record ProcessingConfig(
    int maxChunkSize,           // 最大块大小（字符数）
    int maxChunkSizeOverlap,    // 块之间重叠大小（字符数）
    boolean splitByParagraph,   // 按段落分割
    boolean splitBySentence,    // 按句子分割
    boolean splitByHeading,     // 按标题分割
    String headingPattern,      // 标题匹配模式
    boolean disableChunking     // 禁用分片（整个文档作为一个块）
) {
    public static final ProcessingConfig DEFAULT = new ProcessingConfig(
        2000, 200, true, true, true, "^#{1,6}\\s+", false
    );
    
    public static final ProcessingConfig NO_CHUNKING = new ProcessingConfig(
        Integer.MAX_VALUE, 0, false, false, false, "", true
    );
}
```

---

## 五、关键技术实现

### 5.1 MD5 去重机制

**目的**：避免重复处理相同内容的文档

**实现步骤**：

1. **计算 MD5**：在添加文档前计算 MD5 哼希值
2. **检查重复**：与现有文档块的 MD5 值进行比较
3. **跳过处理**：如果找到相同 MD5 的文档，跳过处理
4. **存储优化**：基于文件路径的文档块不存储内容

**关键代码**：

```java
public void addDocument(Path filePath) {
    String currentMd5 = MD5Util.calculateMD5(filePath);
    
    // 检查是否已存在相同MD5的文档
    boolean alreadyExists = semanticTree.chunks().values().stream()
        .anyMatch(chunk -> chunk.md5() != null && chunk.md5().equals(currentMd5));
    
    if (alreadyExists) {
        logger.info("文件 {} 已存在（MD5: {}），跳过处理", filePath, currentMd5);
        return;
    }
    
    // 创建文件副本并处理
    Path copiedFile = createDocumentCopy(filePath);
    List<DocumentChunk> chunks = processor.processDocument(filePath).get();
    
    // 转换为基于文件路径的文档块
    List<DocumentChunk> filePathBasedChunks = chunks.stream()
        .map(chunk -> DocumentChunk.withFilePath(
            chunk.id(),
            chunk.summary(),
            copiedFile.toString(),
            currentMd5,
            chunk.metadata()
        ))
        .collect(Collectors.toList());
        
    addDocumentChunks(filePathBasedChunks);
}
```

### 5.2 分片存储优化

**问题**：大型语义树导致文件过大、加载缓慢、内存占用高

**解决方案**：

1. **分片策略**：将语义树拆分为多个小文件
2. **按需加载**：只加载当前查询需要的分片
3. **增量更新**：只修改变化的分片

**性能对比**：

| 指标 | 单文件存储 | 分片存储 | 提升 |
|------|----------|---------|------|
| 启动时间（10万文档） | 5.2s | 0.8s | 6.5x |
| 内存占用（10万文档） | 450MB | 120MB | 3.75x |
| 查询响应时间 | 120ms | 45ms | 2.67x |

**最佳实践**：

- 小型知识库（<1000文档）：50-100/分片
- 中型知识库（1000-10000文档）：100-200/分片
- 大型知识库（>10000文档）：200-500/分片

### 5.3 多模型管理

**目的**：根据不同场景使用不同模型，优化成本和效果

**场景分类**：

1. **SUMMARY** - 摘要生成：可以使用较便宜的模型（如 GPT-3.5）
2. **MAP** - 文档映射/聚类：需要较好的理解能力（如 GPT-4）
3. **NAVIGATION** - 导航查询：需要较高的准确性（如 GPT-4-turbo）

**配置示例**：

```yaml
zerovector:
  model:
    summary-model: "gpt-3.5-turbo"
    map-model: "gpt-4"
    navigation-model: "gpt-4-turbo"
```

**使用方式**：

```java
// 获取摘要场景的客户端
ChatClient summaryClient = multiModelManager.getSummaryClient();

// 获取映射场景的客户端
ChatClient mapClient = multiModelManager.getMapClient();

// 获取导航场景的客户端
ChatClient navigationClient = multiModelManager.getNavigationClient();
```

---

## 六、Java 25 特性应用

### 6.1 Records - 不可变数据载体

**优势**：
- 自动生成构造器、getter、equals、hashCode、toString
- 不可变性保证线程安全
- 简洁的语法

**示例**：

```java
public record TreeNode(
    String id,
    String name,
    String description,
    NodeType type,
    List<String> childrenIds,
    List<String> chunkIds
) {}
```

### 6.2 Pattern Matching for switch - 模式匹配

**优势**：
- 简化条件判断逻辑
- 类型安全的模式匹配
- 更易读的代码

**示例**：

```java
switch (action) {
    case SelectChild(String nodeId, String reasoning, double conf) -> {
        if (conf < 0.3) {
            return handleFallback(query);
        }
        current = tree.getNode(nodeId);
        path.add(current);
    }
    case ExpandMultiple(List<String> nodeIds, String reasoning) -> {
        return handleMultiPath(query, nodeIds);
    }
    case FallbackSearch(String reason) -> {
        return handleFallback(query);
    }
    default -> {
        // 处理其他情况
    }
}
```

### 6.3 Virtual Threads - 虚拟线程

**优势**：
- 轻量级线程，可以轻松创建数万个
- 阻塞操作不会阻塞底层线程
- 提高并发性能

**示例**：

```java
try (var executor = Executors.newVirtualThreadPerTaskExecutor()) {
    var futures = chunks.stream()
        .map(c -> executor.submit(() -> summarizeAndExtract(c)))
        .toList();
    
    // 等待所有摘要完成
    futures.forEach(f -> {
        try {
            f.get();
        } catch (Exception e) {
            logger.error("摘要生成失败", e);
        }
    });
}
```

### 6.4 String Templates - 字符串模板

**优势**：
- 更安全的字符串插值
- 支持格式化
- 易于阅读和维护

**示例**：

```java
String prompt = String.format("""
    用户问题: %s
    当前节点: %s
    节点描述: %s
    该节点核心实体: %s
    该节点关键词: %s
    
    请判断应该进入哪个子节点？
    """, query, node.name(), node.description(), entityHints, keywordHints);
```

---

## 七、性能优化策略

### 7.1 mmap 零拷贝读取

**原理**：
- 文件内容直接映射到虚拟内存
- 无需复制到堆内存
- 操作系统按需加载页面

**优势**：
- 极快的读取速度
- 支持远超内存限制的数据量
- 低 GC 开销

### 7.2 分片存储

**原理**：
- 将大型语义树拆分为多个小文件
- 按需加载，减少 I/O 和内存占用
- 并行访问，提高并发性能

**优势**：
- 减少启动时间
- 降低内存占用
- 提高并发性能

### 7.3 虚拟线程并发

**原理**：
- 使用虚拟线程并发处理文档摘要
- 轻量级线程，可以轻松创建数万个
- 阻塞操作不会阻塞底层线程

**优势**：
- 提高构建速度
- 充分利用 CPU 资源
- 简化并发编程

### 7.4 关键词倒排索引

**原理**：
- 维护关键词到节点的倒排索引
- 快速匹配查询中的关键词
- 提供快速通道

**优势**：
- 快速定位相关节点
- 减少 LLM 调用次数
- 提高查询性能

---

## 八、开发指南

### 8.1 环境搭建

**前置要求**：
- JDK 25+
- Maven 3.9+
- IDE（推荐 IntelliJ IDEA）

**步骤**：

1. 克隆项目
```bash
git clone https://github.com/yourusername/zerovector.git
cd zerovector
```

2. 编译项目
```bash
mvn clean compile
```

3. 运行测试
```bash
mvn test
```

### 8.2 添加新功能

**步骤**：

1. 在 `zerovector-core` 模块中添加新功能
2. 编写单元测试
3. 在 `zerovector-spring-boot-starter` 中添加 Spring Boot 支持（如需要）
4. 在 `zerovector-spring-boot-example` 中添加示例（如需要）

### 8.3 代码规范

**命名规范**：
- 类名：大驼峰（PascalCase）
- 方法名：小驼峰（camelCase）
- 常量：全大写，下划线分隔（UPPER_SNAKE_CASE）

**注释规范**：
- 公共 API 必须添加 Javadoc 注释
- 复杂逻辑添加行内注释
- 使用 TODO 标记待完成的功能

### 8.4 测试规范

**单元测试**：
- 使用 JUnit 5
- 测试覆盖率 > 80%
- 每个公共方法都应有测试

**集成测试**：
- 使用 Spring Boot Test
- 测试完整的业务流程
- 使用真实的 LLM 服务（或 Mock）

---

## 九、测试指南

### 9.1 运行测试

**运行所有测试**：
```bash
mvn test
```

**运行特定测试类**：
```bash
mvn test -Dtest=DesignPatternsDocumentTest
```

**运行特定测试方法**：
```bash
mvn test -Dtest=DesignPatternsDocumentTest#testSearchDesignPatterns
```

### 9.2 编写测试

**示例**：

```java
@SpringBootTest(classes = {ZeroVectorSpringBootApplication.class})
@TestMethodOrder(MethodOrderer.OrderAnnotation.class)
public class DesignPatternsDocumentTest {

    @Autowired
    private SemanticFacade semanticFacade;

    @Test
    @Order(1)
    public void testImportDesignPatterns() throws Exception {
        ClassPathResource resource = new ClassPathResource("软件设计模式.md");
        semanticFacade.addDocument(resource.getFile().toPath());
    }

    @Test
    @Order(2)
    public void testSearchDesignPatterns() throws Exception {
        SearchResult result = semanticFacade.search("什么是单例模式");
        assertNotNull(result);
        assertFalse(result.documents().isEmpty());
    }
}
```

---

## 十、故障排查

### 10.1 常见问题

**问题 1：启动失败，提示找不到 JDK 25**

**解决方案**：
- 确认已安装 JDK 25
- 设置 JAVA_HOME 环境变量
- 检查 Maven 配置

**问题 2：文档添加失败，提示文件不存在**

**解决方案**：
- 确认文件路径正确
- 检查文件权限
- 使用绝对路径

**问题 3：查询结果为空**

**解决方案**：
- 确认已添加文档
- 检查 LLM 服务配置
- 查看日志获取详细信息

**问题 4：内存占用过高**

**解决方案**：
- 启用分片存储
- 调整 JVM 内存参数
- 减少文档块大小

### 10.2 日志分析

**日志位置**：
- `logs/app.log` - 应用日志
- `zerovector-spring-boot-example/logs/app.log` - Spring Boot 示例日志

**日志级别**：
- ERROR - 错误信息
- WARN - 警告信息
- INFO - 一般信息
- DEBUG - 调试信息

**调整日志级别**：

```yaml
logging:
  level:
    cn.nexon.zerovector: DEBUG
```

---

## 附录

### A. 相关文档

- [README.md](README.md) - 项目介绍

### B. 参考资料

- [Java 25 Documentation](https://openjdk.org/projects/jdk/25/)
- [LangChain4j Documentation](https://docs.langchain4j.dev/)
- [Spring Boot Documentation](https://spring.io/projects/spring-boot)

### C. 贡献者

感谢所有为本项目做出贡献的开发者！

---

**ZeroVector** - 让知识检索回归本质，简单、高效、智能。
